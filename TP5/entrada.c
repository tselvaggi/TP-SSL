int sumar(int);
int sumar(int* dsgd);//Doble declaracion con distintos parametros
int* sumar(int);//Doble declaracion distinto tipo

int ***saltosolo[], **multiplicarsolo[][], **sumarsolo[][][];//dimensiones variadas en variables

int* cSong(char**, int*);//dimensiones variadas en declaracion de funcion
//COMENTARIO

char multiplicar(char, int cucu);

float salto()//declaracion y definicion al mismo tiempo
{
    for(0=1;i<21;i++) sumar(5);//i no declarada, l no modificable

    while(1){
        int c;
    }
    
    return 5.5=45.0*sumar('b');//error  de tipo en invocacion, por lo que desencadena errores en los tipos de la suma
    sumar(5,5,5);//exceso parametros en invocacion

}

int main()
{
/*
So, so you think you can tell
Heaven from hell?
Blue skies from pain?
*/
    multiplicar('o',53);//invocacion correcta
    float ttt, f[1] = 1;//array inicializado mal
    int array[1] = {1};//array inicializado bien
    int valor = 21/('a'-'a');//error de tipos en division y no se puede dividir por 0
    int b, c, d;// declaracion de varias variables en una linea, el c es error de doble declaracion
    return salto()+(200+d);//salto es float y lo otro es int entonces error de tipos en suma
}

char multiplicar(char coco){//error sematico definicion, no define, ya que tiene mal cantidad de parametros
    int int alfa;//error sintactico doble identificador detipo
    in$t alfa;//error lexico y sintactico
}

int sumar(int ruido)//definicion
{
    int "solo",a,b,a,b,a,b,a,b,as,da,sds,sfd,f;//error sintactico string en vez de identificador
}

int sumar(int pizza)//doble definicion sumar
{
    char a = "a ;//Error lexico "
}
